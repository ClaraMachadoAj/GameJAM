extends Sprite2D

# Called when the node enters the scene tree for the first time.
func _ready():
	self_modulate = Color(0.25, 0.25, 0.25, 1) # basically decrease this value to emulate a shader effect 

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
